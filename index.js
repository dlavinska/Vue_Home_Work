const findSummary = (a, b) => {
  const sum = a + b;
  return sum;
};

const findSubstract = (a, b) => {
  const sub = a - b;
  return sub;
};

const findMultiply = (a, b) => {
  const mult = a * b;
  return mult;
};

findSummary(2, 4);
findSubstract(6, 2);
findMultiply(5, 4);
